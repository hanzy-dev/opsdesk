openapi: 3.0.3
info:
  title: OpsDesk API
  version: 0.1.0
  description: |
    OpenAPI specification for the current OpsDesk backend implementation.

    The API provides a simple serverless helpdesk workflow for:
    - health checks
    - authenticated identity lookup
    - ticket creation
    - ticket listing
    - ticket detail lookup
    - ticket status updates
    - ticket comments

    All timestamps are returned in UTC using RFC3339 / ISO 8601 format.
    RBAC is enforced from Cognito groups reporter, agent, and admin.

servers:
  - url: https://ezkjgr2we9.execute-api.ap-southeast-1.amazonaws.com/dev/v1
    description: Deployed API Gateway URL for the OpsDesk dev environment

tags:
  - name: System
  - name: Auth
  - name: Tickets

paths:
  /health:
    get:
      tags:
        - System
      summary: Check backend health
      operationId: getHealth
      responses:
        "200":
          description: Backend is reachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthSuccessResponse"
              example:
                data:
                  status: ok
                  env: dev

  /auth/me:
    get:
      tags:
        - Auth
      summary: Get authenticated identity
      operationId: getAuthenticatedIdentity
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Authenticated identity returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthIdentitySuccessResponse"
        "401":
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: invalid_token
                  message: authentication token is invalid or expired

  /tickets:
    get:
      tags:
        - Tickets
      summary: List tickets
      operationId: listTickets
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          required: false
          schema:
            $ref: "#/components/schemas/TicketStatus"
          description: Optional status filter.
        - in: query
          name: priority
          required: false
          schema:
            $ref: "#/components/schemas/TicketPriority"
          description: Optional priority filter.
      responses:
        "200":
          description: Ticket list returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketListSuccessResponse"
              example:
                data:
                  - id: TCK-0002
                    title: API Gateway timeout
                    description: Investigate repeated timeout errors
                    status: in_progress
                    priority: high
                    reporterName: Cloud Lab
                    reporterEmail: lab@example.com
                    comments:
                      - id: CMT-0001
                        ticketId: TCK-0002
                        message: Initial triage started
                        authorName: Support Engineer
                        createdAt: "2026-04-14T09:15:00Z"
                        updatedAt: "2026-04-14T09:15:00Z"
                    createdAt: "2026-04-14T09:00:00Z"
                    updatedAt: "2026-04-14T09:15:00Z"
    post:
      tags:
        - Tickets
      summary: Create a ticket
      operationId: createTicket
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTicketRequest"
            example:
              title: API Gateway timeout
              description: Investigate repeated timeout errors in the ticket service
              priority: high
              reporterName: Cloud Lab
              reporterEmail: lab@example.com
      responses:
        "201":
          description: Ticket created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketSuccessResponse"
        "400":
          description: Invalid JSON or validation failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                invalidJson:
                  value:
                    error:
                      code: invalid_json
                      message: request body must be valid JSON
                validationFailed:
                  value:
                    error:
                      code: validation_failed
                      message: request validation failed
                      details:
                        - field: priority
                          message: priority must be one of: low, medium, high
        "403":
          description: Authenticated but not allowed to create a ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /tickets/{id}:
    get:
      tags:
        - Tickets
      summary: Get ticket detail
      operationId: getTicket
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: Ticket detail returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketSuccessResponse"
        "404":
          description: Ticket was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: ticket_not_found
                  message: ticket not found
        "403":
          description: Authenticated but not allowed to view this ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /tickets/{id}/status:
    patch:
      tags:
        - Tickets
      summary: Update ticket status
      operationId: updateTicketStatus
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicketStatusRequest"
            example:
              status: resolved
      responses:
        "200":
          description: Ticket status updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketSuccessResponse"
        "400":
          description: Invalid JSON or validation failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: validation_failed
                  message: request validation failed
                  details:
                    - field: status
                      message: status must be one of: open, in_progress, resolved
        "404":
          description: Ticket was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Authenticated but not allowed to update ticket status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /tickets/{id}/comments:
    post:
      tags:
        - Tickets
      summary: Add a comment to a ticket
      operationId: addTicketComment
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddCommentRequest"
            example:
              message: Investigation has started
              authorName: Support Engineer
      responses:
        "201":
          description: Comment created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommentSuccessResponse"
        "400":
          description: Invalid JSON or validation failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: validation_failed
                  message: request validation failed
                  details:
                    - field: message
                      message: message is required
        "404":
          description: Ticket was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Authenticated but not allowed to comment on this ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    TicketId:
      in: path
      name: id
      required: true
      description: Ticket identifier.
      schema:
        type: string
        example: TCK-0001

  schemas:
    TicketStatus:
      type: string
      enum:
        - open
        - in_progress
        - resolved
      description: Allowed ticket status values.

    TicketPriority:
      type: string
      enum:
        - low
        - medium
        - high
      description: Allowed ticket priority values.

    Timestamp:
      type: string
      format: date-time
      description: UTC timestamp in RFC3339 / ISO 8601 format.
      example: "2026-04-14T09:00:00Z"

    FieldError:
      type: object
      required:
        - field
        - message
      properties:
        field:
          type: string
          example: status
        message:
          type: string
          example: status must be one of: open, in_progress, resolved

    ErrorBody:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: validation_failed
        message:
          type: string
          example: request validation failed
        details:
          type: array
          items:
            $ref: "#/components/schemas/FieldError"

    ErrorResponse:
      type: object
      required:
        - error
      properties:
        error:
          $ref: "#/components/schemas/ErrorBody"

    Health:
      type: object
      required:
        - status
        - env
      properties:
        status:
          type: string
          example: ok
        env:
          type: string
          example: dev

    AuthIdentity:
      type: object
      required:
        - subject
        - username
        - email
        - tokenUse
        - role
      properties:
        subject:
          type: string
          example: 9b0dc1d7-2f5c-40ff-97b5-0201ee2fe2a7
        username:
          type: string
          example: user@example.com
        email:
          type: string
          format: email
          example: user@example.com
        name:
          type: string
          example: OpsDesk User
        tokenUse:
          type: string
          example: id
        role:
          type: string
          enum:
            - reporter
            - agent
            - admin
        groups:
          type: array
          items:
            type: string

    Comment:
      type: object
      required:
        - id
        - ticketId
        - message
        - authorName
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          example: CMT-0001
        ticketId:
          type: string
          example: TCK-0001
        message:
          type: string
          example: Investigation has started
        authorName:
          type: string
          example: Support Engineer
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"

    Ticket:
      type: object
      required:
        - id
        - title
        - description
        - status
        - priority
        - reporterName
        - reporterEmail
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          example: TCK-0001
        title:
          type: string
          example: API Gateway timeout
        description:
          type: string
          example: Investigate repeated timeout errors in the ticket service
        status:
          $ref: "#/components/schemas/TicketStatus"
        priority:
          $ref: "#/components/schemas/TicketPriority"
        reporterName:
          type: string
          example: Cloud Lab
        reporterEmail:
          type: string
          format: email
          example: lab@example.com
        comments:
          type: array
          items:
            $ref: "#/components/schemas/Comment"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"

    CreateTicketRequest:
      type: object
      required:
        - title
        - description
        - priority
        - reporterName
        - reporterEmail
      properties:
        title:
          type: string
          example: API Gateway timeout
        description:
          type: string
          example: Investigate repeated timeout errors in the ticket service
        priority:
          $ref: "#/components/schemas/TicketPriority"
        reporterName:
          type: string
          example: Cloud Lab
        reporterEmail:
          type: string
          format: email
          example: lab@example.com

    UpdateTicketStatusRequest:
      type: object
      required:
        - status
      properties:
        status:
          $ref: "#/components/schemas/TicketStatus"

    AddCommentRequest:
      type: object
      required:
        - message
        - authorName
      properties:
        message:
          type: string
          example: Investigation has started
        authorName:
          type: string
          example: Support Engineer

    HealthSuccessResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/Health"

    AuthIdentitySuccessResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/AuthIdentity"

    TicketSuccessResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/Ticket"

    TicketListSuccessResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Ticket"

    CommentSuccessResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/Comment"
